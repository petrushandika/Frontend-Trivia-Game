import { View, Text } from 'react-native'
import Login from '../screen/LoginScreen'

const setting = () => {
    return (
        <View>
            <Login navigation={''} />
        </View>
    )
}

export default setting